document.addEventListener('DOMContentLoaded', function() {
  // All logo interaction functionality has been removed
  // The logo will now only have the continuous rotation animation
  // with no hover or click interactions
}); 